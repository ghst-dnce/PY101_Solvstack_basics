another_set = {"Monday", "Tuesday", "Wednesday"}

# write one line of code below to add the rest of the weekday strings

# Add the rest of the weekday strings to the set

another_set.update({"Thursday", "Friday", "Saturday", "Sunday"})

print(another_set)